@{
    ViewData["Title"] = "Home Page";
}

<h2>Welcome to the Contract Claim System</h2>
<p>Please choose your role to log in:</p>

<div>
    <a href="@Url.Action("Login", "Account", new { role = "Admin" })" class="btn btn-primary">Login as Admin</a>
</div>
<div>
    <a href="@Url.Action("Login", "Account", new { role = "User" })" class="btn btn-primary">Login as User</a>
</div>

